# Copy to .env and fill in. Never commit .env.
POSTGRES_USER=personal
POSTGRES_PASSWORD=change-me
POSTGRES_DB=personal
# Bearer token required by write endpoints (POST/PUT/DELETE)
API_BEARER_TOKEN=change-me
